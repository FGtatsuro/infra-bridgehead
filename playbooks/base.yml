---
- hosts: all
  remote_user: root
  roles:
    - { role: FGtatsuro.python-requirements }
    - { role: FGtatsuro.ruby }
  tasks:
    - name: root use /bin/bash
      user: name=root shell=/bin/bash
      become: yes
