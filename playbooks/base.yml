---
- hosts: all
  remote_user: root
  roles:
    - { role: FGtatsuro.python-requirements }
    - { role: FGtatsuro.ruby }
  tasks:
    - name: root use /bin/bash
      command: "chsh -s /bin/bash root"
      become: yes
      changed_when: False
